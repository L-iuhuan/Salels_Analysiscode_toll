# 月度分析模板 · 使用 SOP（v2）

## ⚠️ 重要：宏运行方式（2026-09-08 确认）

本机 Excel 为 Build 16.0.20430（新版 Microsoft 365），执行微软收紧后的**网络位置宏强制阻止策略**：
- **网络路径（\\192.168.8.3\...）的 .xlsm 宏会被直接阻止**，用户级"受信任位置/受信任站点"均无法豁免
- **本地路径不受影响**

因此工作方式调整为：

| 用途 | 位置 | 说明 |
|---|---|---|
| **主工作副本（每月在此操作）** | `E:\3-其他资料\数据分析\月度分析模板.xlsm` | 本地路径，宏正常运行（目录已注册受信任位置） |
| 网络归档 | `\\192.168.8.3\...\分析报告-202608\月度分析模板.xlsm` | 只作存档/分发；如需在网络路径直接运行宏，请 IT 在域 GPO 为该目录添加受信任位置 |

**每月流程**：本地副本操作 → 完成后把当月文件另存/上传到网络归档目录（xlsm 或另存 xlsx 干净版均可）。
基础数据文件无论在网络还是本地都能被【① 刷新镜像】读取（只是读数据，不受宏策略影响）。

## 一、结构（27 个 sheet）

| 区域 | Sheet | 说明 |
|---|---|---|
| 说明 | `0-说明` | 名称/公式/指标口径/逆向标定依据 全文档 |
| 控制台 | `0-参数` | 报表年/月、源sheet名、按钮、版本戳、自检、列映射、公式速查 |
| 公式分析层 | `1-`~`11-`、`21-`~`27-`、`增加及流失` | 全部公式，只引用本簿镜像表+参数，**零外部路径** |
| 手工层 | `1b-H1任务项`、`28-长库龄存货明细`、26 的说明列 | 每月手工维护（浅黄/浅灰底色） |
| 数据层 | `D-镜像`（约20万行×16列智能表格） | VBA 从基础数据抽取 |
| 维度层 | `C-维度` | 刷新后自动重建的维度清单 |
| 指标层 | `C-客户指标`、`C-SKU指标`（已隐藏） | 4/21~24 的公式数据源，右键工作表标签可取消隐藏 |

## 二、每月更新流程（约 5-8 分钟）

1. 复制本地模板 → 重命名 `YYYY年M月销售经营分析_数据附件.xlsm`
2. 打开 → `0-参数`：
   - B4 报表年份、B5 报表月份 改为新月份
   - 点【① 刷新镜像】→ 选当月基础数据文件（网络或本地路径均可，任意版本）
   - 等待完成（1-3 分钟），核对版本戳：行数/最大发货日期符合预期
3. 点【② 重算专项】（毛利桥/SKU变化/增加及流失）
4. 核对自检区（B19 行数 OK；C20:C22 参照值填上月附件数）
5. 手工更新：1b 任务项、28 长库龄、26 说明列
6. 归档到网络目录（如需干净版先"另存为 xlsx"自动去宏）

## 三、切换版本/月份 = 零公式改动

- 换数据版本：重复步骤 2（选不同文件即可，公式永不动）
- 跨年：基础数据 sheet 改名（24-26 → 24-27）时只改 `0-参数 B6`
- 回溯验证：选旧版本文件刷新即可复算该版本口径

## 四、新增分析表（三步）

1. 插入新 sheet，从 `C-维度` 复制维度行标签
2. 套用 `0-参数` 速查区公式模式（SUMIFS + 镜像表[列] + 月起/月止/YTD起 等名称）
3. 完成。无外部路径、无跨簿链接。

## 五、维护

- 加镜像列：`0-参数` 列映射配置加一行（源列名须与基础数据表头完全一致）→ 重新【① 刷新镜像】
- 监控型号/专项客户：`9-成本监控 B1`、`7-追觅 B1`（`27-追觅分型号` 同步手工改）
- 建议文本规则阈值：21/22/23 的 IF 公式可直接改

## 六、与 7 月原附件对拍结论（详见 `0-说明`）

- 约 2,600 项比较：1/2b/5/7/8/9 六个 sheet 跨两版数据**逐位一致**
- 其余差异全部定性：附件基于已失传的 7月(8.25) 数据，现存 8.27 版有客户类别/品类重述（四类客户收入总和逐位守恒）
- 隐含口径已固化：低毛利客户=客户SKU毛利率<15%、新品SKU需YTD收入>0、毛利桥量/成本公式等
