# S1+S2 优化核对报告

> 日期：2026-07-26 · 对象：`dashboard/generate_dashboard.py`（后段看板生成器）
> 要求：实施零风险提速项 S1+S2，逐文件核对改动，确保生成看板与原完全一致
> 结论：**通过** —— 后段提速 59.6s(-21.8%)，看板内容与原完全一致（证据如下）

---

## 1. 改动清单（仅 1 个文件、7 处，全部在读取路径上）

文件：`dashboard/generate_dashboard.py`

| # | 位置 | 改动 | 类别 |
|---|---|---|---|
| 1 | L261 | `pd.ExcelFile(path)` → 加 `engine="calamine"` | S1 |
| 2 | L267 | 读"产品快照表" sheet → 加 `engine="calamine"` | S1 |
| 3 | L275 | 读"历史画像追踪" sheet → 加 `engine="calamine"` | S1 |
| 4 | L290 | 读"数据不足" sheet → 加 `engine="calamine"` | S1 |
| 5 | L346-377 | 原始Excel读取重构：**表头先行**(nrows=0, calamine) → 算出 keep_cols → `usecols=keep_cols, engine="calamine"` 精准读取 | S1+S2 |

S2 说明：原逻辑是"全量读 67 列 → 内存里筛 13 列"；新逻辑是"先读表头算列清单 → 只读这 13 列"。列的选择规则（动态匹配"产品线/产品品类（新）/存货名称" + 按索引 24/28/4 取列）**逐字未动**，末尾保留 `rex = rex[keep_cols]` 做列序归一，下游代码面对的数据结构与改造前完全相同。

## 2. 等价性论证

1. 列检测输入从 `rex.columns`（全量读的列）换成 `raw_all_cols`（表头读的列）——两者是同一个文件的同一份表头，内容相同。
2. `usecols` 按文件原顺序返回列，随后 `rex = rex[keep_cols]` 归一为 keep_cols 顺序 —— 与原"全量读后筛选"的最终列序一致。
3. keep_cols 之外的列在改造前于 L380 已被丢弃，下游从未接触。
4. 行数与数据值不受引擎/列裁剪影响（calamine 与 openpyxl 对同一文件解析出的单元格值相同；前段 silver 早已用 calamine 读同一文件并产出经验证的正确结果）。

## 3. 验证证据（实测）

### 3.1 运行指标
- 后段重跑（--skip-processing）：**213.2s**，exit=0，错误流为空（原 272.8s，**-59.6s / -21.8%**）
- `Raw Excel: 191725行 客户:3142 产品:11 存货:818` —— 与改造前 run#4 **逐项一致**
- 列检测：`产品列: '产品线'  品类列: '产品品类（新）'` —— 与 run#4 一致
- 后段自检审计全部 [OK]（KPI占位符/TREND/SA/PIE/SCAT/B_CUSTS/C面DATA）；`[FAIL] Raw=Silver验证` 为**既有良性**审计项（0.4万取整差，run#4 同样存在）
- YTD 4.3亿、毛利率 34.4% 等关键数字与基线一致

### 3.2 看板逐字节核对
- 基线：改动前的 `dashboard_a.html`（SHA256 `BCC18043…9255`，12,809,546 字节）
- 新输出：字节数**完全相同**（12,809,546），哈希不同
- 逐行定位：3897 行中仅 **2 行**不同（L1734 `PROD_CHANGE`、L1761 `F_PRODUCT_LIST`），且两行长度分别完全相等
- 行内定位：差异为**同一组条目的排列顺序不同**（如两个 rev/profit 相同的条目互换位置），所有数值逐值相同

### 3.3 内容等价性证明
对两行做 JSON 解析 + 排序归一（canonical）后逐值比对：
```
PROD_CHANGE:    39 项  → canonical 完全一致
F_PRODUCT_LIST: 426 项 → canonical 完全一致
VERDICT: CONTENT IDENTICAL (order-only difference)
```

### 3.4 证伪实验（顺序差异与本次改动无关）
用**改动前的旧代码**在新进程重跑一次，其输出与基线相比：**同样只在这 2 行上出现顺序差异，规范化后内容完全一致**。
结论：该顺序差异是**既有运行间不确定性**（相同排序键的相对顺序受进程哈希种子影响，每次运行本就可能不同），**不是 S1+S2 引入的**。三方（基线 ≡ 旧代码重跑 ≡ 新代码）内容完全一致。

## 4. 逐文件 diff 核对（与 工作文件\semiconductor_analysis\ 原始件对比）

| 包内文件 | 差异内容 | 判定 |
|---|---|---|
| `dashboard/generate_dashboard.py` | ①迁移期：新增 `load_product_report()` + `build_c_data` 扩展签名（C面统一数据源，前序工作）；②本次：S1+S2 共 7 处（见第1节） | 全部为预期改动 ✓ |
| `processing/analysis/pricing/pricing_customer.py` | L196 pandas-3 `idxmax` 兼容修复（全NaN组预剔除，行为与 pandas 2.x 完全一致） | 已知必要修复 ✓ |
| `processing/config/settings.py` | 新增 PKG_ROOT，DATA_DIR/OUTPUT_DIR 指向包根（单输入/单输出改造） | 已知结构性改动 ✓ |
| 其余全部文件 | 与原始件完全一致（打包期已做零硬编码校验） | ✓ |

原始 diff 存档：本目录 `diffs_S1S2/`（4 份）。

## 5. 性能结果

| 指标 | 改造前 | 改造后 | 变化 |
|---|---|---|---|
| 后段耗时 | 272.8s | 213.2s | **-59.6s (-21.8%)** |
| 全链耗时（前段+后段） | 560.4s | ~500s | **约 -60s** |
| 后段内存峰值（读全表→读13列） | 高 | 显著降低 | 67列→13列 |
| 输出内容 | 基线 | 与基线完全一致 | 0 差异 |

## 6. 复现实验方法（备查）

1. 基线冻结：改动前复制 `dashboard_a.html` 并记录 SHA256
2. 改动后 `--skip-processing` 重跑后段，比对行数/审计/哈希
3. 逐行 diff 定位差异行；行内前缀/后缀扫描定位差异区间（O(n)，勿用 difflib 整行比对，百万字符行会 O(n²) 超时）
4. JSON 规范化比对证明内容等价
5. 旧代码证伪重跑证明顺序差异为既有不确定性
