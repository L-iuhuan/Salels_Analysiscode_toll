# 数据分析工作区 · 使用说明(2026-07-27 重组后)

## 一、目录结构

```
E:\3-其他资料\数据分析\
├─ sales_analytics_platform\   ← 主平台:数据处理 + 看板(③骨架+②全部更新,已验证)
├─ forecasting\                ← 预测系统
│  ├─ quarterly\               ← 季度预测包(①权威版,CLI: --data/--sheet/--config)
│  ├─ unified\                 ← 统一预测定版 unified_forecast_system.py
│  │  ├─ output\               ← 统一预测产物(product/customer_path_forecast.csv)
│  │  └─ _archive\             ← 淘汰版本(v3 因月度/季度单位bug淘汰,见裁决文档)
│  ├─ utils\                   ← chart_data / final_forecast / generate_chart_html
│  └─ _archive\                ← eda/longtail全族/quantile实验/optimizer
├─ project_analysis\           ← 重组工程档案(清单/卡片/方案/验证报告/对比证据/工具)
├─ _archive_source\            ← 全部源副本(只读保存,勿改)
│  ├─ semiconductor_analysis\        (=① 含 .codegraph 索引)
│  ├─ semiconductor_analysis_before\ (=④ 5/20快照)
│  ├─ 看板流水线\                     (=③ 便携版原身)
│  ├─ 工作文件\semiconductor_analysis\ (=② 活跃主干原身)
│  ├─ 工作文件\                       (散件: eda脚本/csv/html/5月数据副本/longtail/审批流)
│  ├─ project_branches\              (阶段五分支快照9个,含_orphans)
│  ├─ 产品生命周期评估\ 客户销售情况分析\ experiment_log\
├─ 工作文件\                    ← 仅剩 semiconductor_analysis.rar(登记保留)
├─ 产品生命周期评估.rar           ← 登记保留,未动
├─ PROJECT_CHARTER.md             ← 项目宪法(2026-08-18 council裁决:红线R1-R4/强规S1-S6)
└─ CoStrict\                    ← 未纳入本次重组,未动
```

## 二、日常使用

### 1. 跑经营看板(月度例行动作)
1. 把当月 **财务分析-x月.xlsx** 和 **部门-人员-职务对应.md** 放进 `sales_analytics_platform\data\`
   （`data\` 下可放多份历史文件，程序自动取最新 mtime 的一份"财务分析-*.xlsx"，请确保当月文件最新）
2. **人工核对加工完成后、任何人在 Excel 里打开/保存原文件之前**，先在明文窗口跑快照 ingest：
   ```
   cd 仓库根目录
   python scripts\ingest_snapshot.py
   ```
   作用：把当月 ERP 明文转成 `sales_analytics_platform\data_warehouse\YYYYMM\erp_snapshot.parquet` 快照。
   **之后文件即使被 DSE 加密，跑批也照常**（直接从快照读，不碰加密件）。
   > 若源文件已被加密且没跑过 ingest，跑批会明确报错并提示先运行本命令（不会静默崩英文）。
3. 双击 `sales_analytics_platform\` 下的中文 bat,或命令行:
   ```
   cd sales_analytics_platform
   python run_chain.py --data "data\财务分析-x月(x.x).xlsx" --force-silver
   ```
4. 看板产出: `dashboard\dashboard_a.html`(双击浏览器打开);中间表在 `output\`

> **--force-silver 怪癖已修复（批次①, 2026-08-18）**:此前 silver 缓存命中时 customer 阶段
> 拿不到带"未知客户"填充的 raw 数据,导致客户归属(业务员/渠道)与全新运行不一致。
> 批次①已统一缓存/新鲜两条路径(golden-diff 零漂移验证),两种模式产出一致。
> `--force-silver` 仍可用于强制重算,但不再是正确性必需。

> **看板快速迭代(批次③/④a, 2026-08-24)**:只调看板代码/模板/截断时,用
> `python run_chain.py --dashboard-only`——预聚合指纹新鲜则秒级出看板(约1s);
> 若数据/配置/代码已变化,会中文报错引导先全量跑(拍板结论:过期不静默用旧数据)。
> 注意:新月份数据更新必须走全量跑,快速模式只为看板侧迭代服务。

### 2. 跑统一预测(产品+客户双路径)
```
cd forecasting\unified
python unified_forecast_system.py            (自动取平台 data\ 下最新 xlsx)
python unified_forecast_system.py "数据路径"  (显式指定)
```
产物: `forecasting\unified\output\product_path_forecast.csv` / `customer_path_forecast.csv`
依赖: `forecasting\quarterly\output\quarterly_forecast_customer\预测方法排行榜.csv`(已就位;重跑 quarterly 会刷新它)

### 3. 跑季度预测包
```
cd forecasting\quarterly
python run_quarterly_forecast.py --data "数据路径" --sheet "24-26" --config forecast_config.default.json
python run_customer_forecast.py  --data "数据路径" --sheet "24-26" --config forecast_config_customer.json
```

### 4. 发布到共享盘(改了平台代码后必做)
改了 `sales_analytics_platform\` 里的任何代码/配置,收工前必须发布(或在施工台账登记"未同步原因"):
```
powershell -ExecutionPolicy Bypass -File kanban\tools\publish_to_share.ps1
```
- 脚本会先自检:共享盘已最新会提示,落后会打印落后版本;同步后复读 version.txt 验证
- 共享盘 UNC 路径在 `kanban\share_config.json` 单点配置;壳端(lib.rs)发版时需核对一致
- 客户端下次启动或点「同步代码」即拉到新版;报问题时把 version.txt 和壳版本号都带上
- 共享盘只出核心代码:数据/产出/看板 html/快照仓一律不同步(publish 排除清单保证)

## 三、关键认知(踩坑记录)

| 事项 | 说明 |
|---|---|
| **主表是 "24-26"** | xlsx 中 "总表" sheet 已冻结于 2026-05(不再更新);"24-26" 才是月度刷新的活跃交易表。平台与统一预测均已对齐到 "24-26" |
| **--force-silver 强制**(已解除) | 批次①(2026-08-18)已修复缓存怪癖:缓存复跑与全新运行产出一致(golden-diff 零漂移)。原怪癖记录见 `project_analysis\m3_verification.md` |
| 控制台中文乱码 | 用 `$env:PYTHONIOENCODING='utf-8'` 或 bat(已内置 chcp 65001);写文件内容不受影响 |
| 环境 | Python 3.11 + pandas 2.3.1(平台 pricing 已做 pandas 2.3/3.x 双向兼容:修改#5) |
| 平台 README | `sales_analytics_platform\README.md` 是③原《看流水线·小白版》,步骤仍有效,文中的"看板流水线文件夹"即现在的 `sales_analytics_platform` |

## 四、重组工程档案(project_analysis\)

- `07_merge_plan.md` — 合并方案终稿(D1-D4 决策/原子覆盖组/修改清单/验证协议)
- `深度分析与修复建议报告.md` — 全量诊断(2026-08-06,自 sales_analytics_platform 移入)
- `深度分析评审裁决-2026-08-18.md` — P0四项裁决+四批次施工方案(自 sales_analytics_platform 移入)
- `项目诊断报告_20260815.md` — 项目诊断报告(自根目录移入)
- `施工总计划_20260818.md` — council裁决终稿:批次⓪-④b/验收标准/测试方案/加速裁决/GitHub同步
- `m3_verification.md` — 平台验证报告:全链通过;golden-diff vs ②基线(silver 逐字节一致,gold 28/30 字节级一致+2 非回归残留);缓存怪癖全记录
- `unified_verdict.md` — 统一预测定版裁决:system 胜(8.92亿=TTM~105%);v3 因月度值写入季度桶(总量仅 36%)淘汰
- `unified_compare\` — 定版对比证据;`_tools\` — 全部可复跑脚本

## 五、修改台账(全部配置级,共5+1处)

| # | 位置 | 内容 |
|---|---|---|
| 1-3 | forecasting\unified\unified_forecast_system.py | DATA_FILE/RANKING_FILE/OUTPUT_DIR 绝对路径→相对+CLI;SHEET_NAME "总表"→"24-26" |
| 4 | sales_analytics_platform\run_chain.py | ensure_output_junction 增加 data 联接+运行前自检(council P0) |
| 5 | platform pricing_customer.py | groupby 补 observed=True(③的 pandas-3 修复在 2.3.1 下反引入崩溃;改后与②输出逐值一致) |
| — | test/ 落包根 + 根 conftest.py 注入 processing/ | 结构性安置(council P0) |

## 六、Git 版本链(仓库根=本目录)

```
406c1ed v0 重组前快照 → 6e7d3d6 M1 平台合并 → 57d55e1 M2 预测合并
→ 48e43ac M3 平台验证通过 → be6880b unified定版 → e2b31e0 修改#1-3
→ 834c825 M4 源副本归档
```
回滚任意阶段:`git log` / `git checkout <hash>`(数据与产出经 .gitignore 排除,不入库)。
