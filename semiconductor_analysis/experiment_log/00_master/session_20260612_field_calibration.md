# 会话记录: 字段校准与实验暂停

## 时间
- 2026-06-12

## 触发原因
用户指出客户字段使用存在疑问，要求暂停盲目重跑，先校准数据字段取用。

## 已确认问题
1. `quarterly_forecast_package/run_quarterly_forecast.py` 原先使用 `代理商/直供名称` 作为产品×客户聚合键，不符合最终客户口径。
2. 正确客户输出名称应为 `终端客户简称`。
3. `终端客户名称_客户类别` 是客户分层字段，不是客户名称字段。
4. 实验1.0当前结果不可作为结论使用，原因包括：
   - 客户字段口径此前不一致；
   - 实验1.0脚本仍存在 `代理商/直供名称` 残留；
   - 当前实验1.0脚本实际复用了季度桶底层方法层级，尚未严格实现测试方案中的“产品线×月度→季度汇总、品类×月度→产品线汇总、SKU×月度→产品线汇总”。

## 已执行动作
1. 已暂停继续重跑。
2. 已终止误启动的 Python 重跑进程。
3. 已扫描以下范围字段使用：
   - `quarterly_forecast_package/**/*.py`
   - `experiment_log/**/*forecast*.json`
   - `experiment_log/**/*run*.py`
4. 已暂时修改：
   - `quarterly_forecast_package/run_quarterly_forecast.py`: 将产品×客户聚合键相关 `代理商/直供名称` 替换为 `终端客户简称`。
   - `experiment_log/03_exp_0.1_env_setup/output/forecast_config.locked.json`: 将字段映射中的 `代理商/直供名称` 改为 `终端客户简称`。

## 当前状态
- 暂不继续运行实验。
- 先提交字段口径清单给用户确认。
- 用户确认后，再决定是否：
  1. 保留上述修改；
  2. 修正实验1.0脚本；
  3. 重跑0.2和1.0；
  4. 将旧结果标记为作废。

## 待用户确认
字段口径表、客户字段、产品/SKU字段、品类字段、客户分层字段、金额/成本/利润字段。