# 字段梳理文档 - Phase 1实验修正

## 创建日期: 2026-06-15
## 修正原因: 之前字段选择错误导致产品线不匹配

---

## 1. 数据源和读取方法

### 主数据源
- **文件路径**: `E:\3-其他资料\数据分析\semiconductor_analysis\data\财务分析-5月（6.3）.xlsx`
- **工作表名称**: `总表`
- **数据量**: 331,402行，67列
- **数据类型**: 原始ERP出货明细数据

### 推荐读取方法
```python
import pandas as pd
from pathlib import Path

PROJECT_ROOT = Path(r'E:\3-其他资料\数据分析\semiconductor_analysis')
DATA_FILE = PROJECT_ROOT / 'data' / '财务分析-5月（6.3）.xlsx'

# 使用calamine引擎（Rust实现，速度是openpyxl的5-10倍）
df = pd.read_excel(DATA_FILE, sheet_name='总表', engine='calamine')

# 如果calamine不可用，回退到openpyxl
# df = pd.read_excel(DATA_FILE, sheet_name='总表')
```

### calamine引擎安装
```bash
pip install python-calamine
```

### 数据验证
```python
# 验证数据加载成功
print(f"数据行数: {len(df)}")
print(f"列数: {len(df.columns)}")
print(f"产品线数量: {df['型号_产品线（新）'].nunique()}")
```

---

## 2. 正确的字段映射（对照1.1实验脚本验证）

### 核心字段

| Excel原始字段 | 派生字段名 | 用途 | 来源 |
|---|---|---|---|
| `型号_产品线（新）` | 产品线名称 | **产品线标识** | 1.1实验脚本:104行 |
| `存货编码` | SKU预测键 | SKU标识 | 1.1实验脚本:137行 |
| `存货名称` | SKU预测键 | SKU标识（备选） | 1.1实验脚本:139行 |
| `终端客户简称` | 预测客户名称 | 客户标识（首选） | 1.1实验脚本:144行 |
| `代理商/直供名称` | 预测客户名称 | 客户标识（备选1） | 1.1实验脚本:146行 |
| `实际终端客户` | 预测客户名称 | 客户标识（备选2） | 1.1实验脚本:148行 |
| `终端客户名称_客户类别` | 预测客户名称 | 客户标识（备选3） | 1.1实验脚本:150行 |
| `RMB 未税金额小计` | 销售额 | 销售金额 | 1.1实验脚本:204行 |
| `发货数量` | 销量 | 销售数量 | 1.1实验脚本:205行 |
| `发货日期` | 日期 | 时间基准 | 1.1实验脚本:110行 |
| `总成本` | 成本 | 成本金额 | 1.1实验脚本:153行 |
| `利润` | 利润 | 利润金额 | 1.1实验脚本:115行 |
| `ERP订单号` | 订单号 | 订单标识 | 1.1实验脚本:206行 |

### 辅助字段

| Excel原始字段 | 用途 | 备注 |
|---|---|---|
| `产品品牌` | 产品分类（一级） | 用于13条产品线分类 |
| `产品系列` | 产品分类（二级） | 用于品类细分 |
| `型号_产品品类` | 品类代码 | 用于产品生命周期参照组 |
| `是否新品` | 新品标记 | 用于新品/老品分层 |
| `未税单价` | 单价 | 用于价格分析 |

---

## 2. 字段映射规则

### 产品线字段映射
```python
# 关键修正：使用"型号_产品线（新）"而不是"产品品牌"
# 产品品牌有13种，而型号_产品线（新）有17种，与基线数据匹配

# 正确映射
df['产品线名称'] = df['型号_产品线（新）'].fillna('未分类')

# 错误映射（之前使用的）
# df['产品线名称'] = df['产品品牌']  # 这会导致产品线不匹配
```

### 客户字段映射
```python
# 预测客户名称 = 终端客户简称 → 代理商/直供名称 → 实际终端客户 → 未知终端客户
df['预测客户名称'] = df['终端客户简称'].astype(str).str.strip()
mask_nan = df['预测客户名称'].isna() | (df['预测客户名称'] == '')
df.loc[mask_nan, '预测客户名称'] = df.loc[mask_nan, '代理商/直供名称'].astype(str).str.strip()
# ... 以此类推
```

### SKU预测键映射
```python
# SKU预测键 = 存货编码 → 存货名称 → 未知SKU
df['SKU预测键'] = df['存货编码'].astype(str).str.strip()
mask_sku_missing = df['SKU预测键'].isna() | (df['SKU预测键'] == '')
df.loc[mask_sku_missing, 'SKU预测键'] = df.loc[mask_sku_missing, '存货名称'].astype(str).str.strip()
```

---

## 3. 产品线数量验证

### Excel中的产品线（型号_产品线（新））
- 数量：17种
- 包括：POE电源管理、dTOF模组、充电与控制电源管理、新显示MLED驱动、无刷直流电机驱动、有刷直流电机驱动、未分类、步进电机驱动、电机驱动、电源模组、电脑&计算电源管理、硬件锂电保护、磁传感、车规电机驱动、车规电源管理、通用电源管理、音频功放

### 基线数据中的产品线
- 数量：17种
- 与Excel中的"型号_产品线（新）"字段完全匹配

### 错误的产品线（产品品牌字段）
- 数量：13种
- 包括：电机驱动、电源模组、POE、音频功放、信号链-高速、DC-FAN、车规电源、LocalDimming、数字电源、MOS等
- **不匹配基线数据**

---

## 4. 数据验证检查点

### 必须验证的检查点
1. 产品线数量必须为17种
2. 产品线名称必须与基线数据完全匹配
3. 销售额字段不能有NaN或负值
4. 日期字段必须为有效日期
5. 客户字段不能全为空

### 验证代码
```python
# 验证产品线数量
assert df['产品线名称'].nunique() == 17, f"产品线数量错误: {df['产品线名称'].nunique()}, 应为17"

# 验证产品线名称
expected_lines = ['POE电源管理', 'dTOF模组', '充电与控制电源管理', '新显示MLED驱动', 
                  '无刷直流电机驱动', '有刷直流电机驱动', '未分类', '步进电机驱动', 
                  '电机驱动', '电源模组', '电脑&计算电源管理', '硬件锂电保护', 
                  '磁传感', '车规电机驱动', '车规电源管理', '通用电源管理', '音频功放']
actual_lines = df['产品线名称'].unique()
missing_lines = set(expected_lines) - set(actual_lines)
assert len(missing_lines) == 0, f"缺少产品线: {missing_lines}"

# 验证销售额
assert df['销售额'].notna().all(), "销售额有NaN"
assert (df['销售额'] >= 0).all(), "销售额有负值"

# 验证日期
assert df['日期'].notna().all(), "日期有NaN"
```

---

## 5. 修正记录

### 2026-06-15 修正
- **问题**: 使用"产品品牌"字段导致产品线数量为13种，与基线数据不匹配
- **原因**: 未对照之前1.1实验脚本验证字段选择
- **修正**: 改用"型号_产品线（新）"字段，产品线数量正确为17种
- **影响**: 之前所有使用错误字段的测试结果均不可信，需要重新执行

### 下一步
1. 修正实验1.1的字段映射
2. 重新执行覆盖率边界规则筛选
3. 重新执行所有Phase 1实验

---

## 8. Phase 1修正完成记录

### 完成时间: 2026-06-15

### 已完成的修正
1. ✅ 字段映射修正: 使用"型号_产品线（新）"代替"产品品牌"
2. ✅ 产品线数量验证: 正确为17种
3. ✅ 实验1.2: 产品级窗口优化
4. ✅ 实验1.3B: PIT代理层验证

### 实验1.2产品级窗口优化结果

| 产品线名称 | 最优窗口_产品级 | 最优WAPE_产品级 |
|---|---|---|
| 新显示MLED驱动 | 6 | 2.34 |
| 无刷直流电机驱动 | 6 | 0.31 |
| 电源模组 | 3 | 0.04 |

### 实验1.3B PIT代理层验证结果

| 产品线名称 | proxy_risk_score | top1_customer_share | sales_growth_12m |
|---|---|---|---|
| 新显示MLED驱动 | 60 | 100% | 0% |
| 无刷直流电机驱动 | 30 | 60% | 2469% |
| 未分类 | 30 | 71% | 400% |
| 电源模组 | 30 | 27% | 0% |

### 关键发现
1. **产品级窗口优化**: C类产品线最优窗口为3-6，与产品线级（12）不同
2. **PIT代理层**: 新显示MLED驱动风险最高（60分），客户集中度100%
3. **数据质量**: 未分类产品线有3个有效季度，标记为不可统计预测

### 输出文件
1. `window_optimization_product_level.csv` - 产品级窗口优化结果
2. `pit_proxy_validation.csv` - PIT代理层验证结果
3. `phase1_correction_report.md` - 修正完成报告