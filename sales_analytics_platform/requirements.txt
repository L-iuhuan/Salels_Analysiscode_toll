# 看板流水线运行依赖
# 安装：pip install -r requirements.txt
# 版本区间按宪法 A3 锁定上限（2026-08-18 批次⓪，基于生产环境已验证版本：
# pandas 2.3.1 / numpy 2.4.0 / openpyxl 3.1.5 / statsmodels 0.14.6 / chinese_calendar 1.11.0
# rapidfuzz 3.14.5 / matplotlib 3.10.3 / scikit-learn 1.8.0 / python-calamine 0.6.2 / xlsxwriter 3.2.9）

pandas>=2.2,<2.3.2      # 后段Excel读取使用calamine引擎（pandas 2.1+才支持，留余量）
                        # [批次⑤ 缺陷B修复] 上限收紧到 <2.3.2：pandas 2.3.2+ 起 category dtype 的
                        # map/索引结果参与算术运算会抛 TypeError（portrait.py 产品线占比除法、
                        # pricing_actions.py 提价空间减法两处已加防御，但仍以生产验证过的 2.3.1 为准）
numpy>=1.26,<3.0
openpyxl>=3.1,<4.0
statsmodels>=0.14,<0.16
chinese_calendar>=1.11,<2.0
rapidfuzz>=3.0,<4.0
matplotlib>=3.7,<4.0
scikit-learn>=1.3,<2.0
python-calamine>=0.2,<1.0  # 必需：前后段Excel读取引擎，缺失会报 Unknown engine: calamine
xlsxwriter>=3.1,<4.0       # 必需：gold 报告 Excel 导出(reports/gold_exporter.py)，漏装会在客户分析阶段崩溃
pyarrow>=14,<21            # 必需：Silver层 Parquet 双写/读取(shared/data_cleaning.py,批次② 车道C；生产验证版本 20.0.0)
pywin32>=306,<312          # 必需：DSE 密文 Excel COM 解密兜底(shared/excel_com.py, r16/r17；生产验证版本 306；便携 Python 需安装)
pyyaml>=6.0,<7.0           # 必需：R面看板 faces.yaml/风险面 yaml 解析(dashboard/generate_risk_face.py 顶层import + generate_dashboard.py 延迟import, r19 干净测试实测缺失致 R面静默降级；生产验证版本 6.0)
