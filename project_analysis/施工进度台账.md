# 施工进度台账（唯一进度事实源）

> **开工先读**：本台账 → `PROJECT_CHARTER.md` → `project_analysis\施工总计划_20260818.md`
> **收工必写**：已完成 / 下一步 / 未决 / 偏离记录。重要决策当日写入"决策记录"，不许只留在对话里。

## 当前状态

| 项 | 值 |
|---|---|
| 当前批次 | **W 系列进行中（W1 快照仓✅ / W2 共享盘同步✅ / W3 叙事设计 v4 定稿✅ / W4 风险与行动测试版：开工）** |
| 分支 | master（常态维护模式，W 系列小批次直接 master 推进） |
| HEAD | 最新；推送随收官执行 |
| 上次更新 | 2026-08-25（日期订正第二轮 + W4 开工） |

## 批次总览

| 批次 | 内容 | 状态 | 开工 tag | 完工 tag | 验收结论 |
|---|---|---|---|---|---|
| ⓪ | baseline 工具链 + 数据契约 + schema 预检 | **完成** | pre-batch-0 | batch-0-done | 零漂移自证通过；基线=2026-06月数据（silver 4 + gold 30 表，共 2,984,033 行；看板 44 变量 / 6969 KPI 点） |
| ① | 缓存正确性 + Silver 口径统一（含 BLOCKER-2） | **完成** | pre-batch-1 | batch-1-done | force 对拍=恰好 6 处已裁决预期漂移；cache 对拍=零漂移；指纹失效/复现通过；[STAGE] 协议零改动 |
| ② | Dashboard 最小修复 + Parquet 双写 + 向量化 | **完成** | pre-batch-2 | batch-2-done | force 1389.9s / cache 1348.0s 双零漂移；HTML 对拍零漂移；2027 纯函数自测 PASS |
| ②.5 | 死代码清理 + 接口契约冻结 + 非确定性治理 | **完成** | pre-batch-2.5 | batch-2.5-done | 全量 799.3s 零漂移；输出跨进程逐字节可复现（双哈希种子验证） |
| ③ | 口径统一（业务门禁）+ 看板预聚合 | **完成** | pre-batch-3 | batch-3-done | 全量 801.7s 零数值漂移（仅 4 个预期新增轴边界变量）；--dashboard-only 新鲜 9.1s / 过期 exit1 |
| ④a | 看板零 Excel 收口（加载底座切 silver）+ 逐面对拍 | **完成** | pre-batch-4a | batch-4a-done | 全量 618.6s 零数值漂移；看板全算 400→100.9s；--dashboard-only 对 data/*.xlsx 读取=0 |
| ④b | run_all 显式 STAGES 注册表化 + 真实契约测试 + 遗留收尾 | **完成** | pre-batch-4b | batch-4b-done | 全量 764.9s 零数值漂移；pytest 7 用例全绿；[STAGE] 协议零改动 |

## 批次⓪ 任务清单（已完成 2026-08-18）

- [x] `scripts\freeze_baseline.py`（+ `_baseline_common.py` 共享采集）
- [x] `scripts\golden_diff.py`（rescan 对比 + --html-old/--html-new 逐字段对拍；数值 1e-6、计数/字符串严格；exit 0/1/2）
- [x] 首次全量跑基线（`--force-silver`，1500.5s，exit=0），摘要入 `baseline\20260818\summary.json` 并提交
- [x] `docs\DATA_CONTRACT.md`（silver 4 张 + gold 30 张逐表核对 + Dashboard 直读 Excel 列专节）
- [x] ~~阶段产物 schema 预检~~ → 见偏离记录（以 golden_diff 严格 schema 对比 + DATA_CONTRACT 替代）
- [x] 依赖补全 + 版本上限锁定（requirements.txt 10 项，按生产已验证版本设区间）
- [x] golden_diff 自证：官方基线 rescan 零漂移（exit=0）
- [x] 验收 → 合入 master → 打 `batch-0-done`（推送待网络）

## 批次① 任务清单（已完成 2026-08-19）

- [x] 切分支 `batch/1-cache-silver` + 打 `pre-batch-1`
- [x] BLOCKER-2：`_prepare_data()` 静默绕过——run() 统一入口，独立/链式两路径同走 silver_cleaned_rows.csv；过滤/钳制改配置开关默认关闭（`_口径_负销量过滤`/`_口径_毛利率钳制`，settings_product.py）
- [x] BLOCKER-4 + P0-2：三级指纹（6 个源码/配置文件 + 源 Excel 身份），含行尾归一化硬化（LF/CRLF 不触发误失效）
- [x] P0-1：统一 `build_cust_info()`（data_cleaning.py），run_all 新鲜路径 + run_pipeline 缓存路径 + run_kpi_daily 缓存重读路径三处归一
- [x] silver avg_price lambda 向量化（等价变换，列顺序保持）
- [x] 阶段失败非零退出（_STAGE_ARTIFACTS + _verify_stage_artifacts；run_chain 已有 returncode 阻断，未改）
- [x] 验收全过：force 对拍=恰好 6 处已裁决预期漂移（见决策记录）；cache 对拍=零漂移；改配置→哈希失效、还原→哈希复现；[STAGE] 协议 git diff 零改动
- [x] 收工：台账更新 → 合入 master → `batch-1-done`（推送待网络）

## 批次② 任务清单（已完成 2026-08-19）

- [x] 切分支 `batch/2-dashboard-minfix` + 打 `pre-batch-2`
- [x] sheet 名读 `DATA_SHEET_NAME`（原 :350,379 硬编码）
- [x] 魔法列号 66/24/28/4 → `resolve_dashboard_col()` 列名语义匹配，`DASHBOARD_COL_PRIORITY` 入 settings.py（找不到报错退出，不静默用错列）
- [x] 年份/H1 周期/模板硬编码 → `derive_periods()` 从 `_latest_period` 动态推导（2027/2025 纯函数自测 PASS；template.html 占位符化）
- [x] 修审计假 FAIL：`Raw=Silver验证` 改真实数值校验（当前数据下 OK）
- [x] 看板内向量化：yd/tyd 12月×4层级循环、产品型号变迁嵌套循环 → 预聚合 dict/groupby
- [x] 处理侧向量化：portrait 65.8→45.4s、trend 19.0→3.1s、rules A1-A6 10-50x、交叉销售只算一次（631→569s）
- [x] CSV+Parquet 双写（含 run_all.py 生产链直写处）；读侧 mtime+dtype 归一双保护回退
- [x] KPI 缓存路径改读 silver_cleaned_rows（优先 parquet）：30-60s Excel 重读 → 1.6s，逐值验证客户数=52817
- [x] 验收：force 1389.9s（-7.3%）/ cache 1348.0s（-8.9%）双零漂移；HTML 对拍零漂移；py_compile 全过
- [x] 收工：台账更新 → 合入 master → `batch-2-done` → 推送

## 批次②.5 任务清单（已完成 2026-08-19）

- [x] 切分支 `batch/2.5-cleanup` + 打 `pre-batch-2.5`
- [x] 死代码清理：9 个文件 git mv 入 `_deprecated\`（core/ 整体、data_pipeline 三 wrapper、b2b_adapters.py、test_pipeline.py）并登记；`--pipeline` 入口移除；validator.py 保留
- [x] 保留项裁定：诊断报告死代码清单部分失准——classifiers/risk_scoring 经 shared/__init__.py re-export 且被 profiling.py/validation_suite.py 实际调用，依"有引用不得删"保留；load_config_from_xlsx 留批次③随 D-4 业务确认
- [x] 非确定性治理：cross_sell set 迭代改 sorted；PROD_CHANGE/F_PRODUCT_LIST 加 name 决胜键——看板输出双哈希种子（1/777777）逐字节一致
- [x] 性能大单点：build_monthly_product_trend 536→1.0s；_compute_batch_metrics 73.5→0.45s（≈162x）；run_action_suggestions 631→15.7s（≈40x）
- [x] 接口契约冻结：DATA_CONTRACT.md 登记冻结点（批次④a 列迁移基线）
- [x] 验收：全量 799.3s + golden_diff 零漂移（对拍 baseline\20260818_batch1）
- [x] 收工：台账更新 → 合入 master → `batch-2.5-done` → 推送

## 批次③ 任务清单（业务门禁：预演已交付 → 等 D-1/D-2/D-4 拍板）

- [x] `scripts\impact_rehearsal.py`：D-1/D-2 组合下收入/毛利/风险分布差异报告（2026-08-24 交付，10.5s 可月度重跑；报告 `project_analysis\口径决策预演_20260824.md`）
- [x] **业务拍板**（2026-08-24 已拍）：D-2 保留 / D-1 仅展示层截断 p1~p99 阈值入配置 / D-4 本期不引入
- [x] 二阶段：看板自缓存 preagg.json + 指纹门禁（--dashboard-only 过期报错退出）+ 展示层截断落地
- [x] 验收：fresh vs cached 看板逐字节一致（SHA256 相同）；--dashboard-only 新鲜 9.1s exit0 / 过期与缺失 exit1 中文报错；全量零数值漂移（仅 4 个预期新增轴边界变量 MARGIN/ASP_AXIS_MIN/MAX，值与预演报告精确一致）

## 批次④a 任务清单（已完成 2026-08-24）

- [x] 切分支 `batch/4a-column-migration` + 打 `pre-batch-4a`
- [x] 6 面逆向工程文档化：`docs\DASHBOARD_FACE_MAP.md`（逐面数据源/缺口分析/迁移顺序）
- [x] 关键发现：13 列 Excel 直读列**全部已在 silver_cleaned_rows**（列级零缺口）→ 方案从"逐面搬迁"调整为"加载底座单点切换"（见偏离记录）
- [x] Step 0 前置门禁：raw vs silver 13 列全字段等价（2 处差异深证无害：日期格式/未知客户 fillna）
- [x] 加载底座：rex 改读 silver_cleaned_rows（优先 parquet），`--from-excel` 显式回退保留
- [x] D面 6n 客户热力图 858 次过滤→1 次 groupby（241.7s→1.4s）；6l/6l2 预聚合
- [x] 收口目标达成：--dashboard-only 对 data/*.xlsx 读取次数=0（运行时监控证实）
- [x] 验收：全量 618.6s 零数值漂移；fresh/cached 逐字节一致；--from-excel 与 silver 两路径输出逐字节一致
- [x] 收工：台账更新 → 合入 master → `batch-4a-done` → 推送

## 批次④b 任务清单（已完成 2026-08-24）

- [x] 切分支 `batch/4b-stage-tests` + 打 `pre-batch-4b`
- [x] run_all.py 重构为显式 STAGES 注册表驱动（模块级注册表 + `_run_stage` 统一分派；RUN_STAGES 选择性执行语义保留；接口名全部保留）
- [x] 真实契约测试 `test\test_run_all.py`（7 用例：指纹决定性×2 / 阶段产物校验×2 / 基线工具自洽 / 看板门禁×2；30-58s 全绿）替代已入 _deprecated 的 mock 测试
- [x] 遗留收尾：rules.py `_safe_val` 全仓零引用已删；DATA_CONTRACT 直读 Excel 专节加历史标记
- [x] 验收：全量 764.9s 零数值漂移（仅批次③已登记的 8 个轴边界新增变量）；pytest 7 passed；[STAGE] 协议 git diff 零命中
- [x] 收工：台账更新 → 合入 master → `batch-4b-done` → 推送

> **2026-08-24：评审裁决书四批次施工方案（⓪①②②.5③④a④b）全部执行完毕。**

## 决策记录

| 日期 | 决策 | 依据/来源 |
|---|---|---|
| 2026-08-18 | 系统定位为未来 12–24 个月**长期演进核心资产** → 四批次全做 | 用户拍板（钢人论证后） |
| 2026-08-18 | 采用渐进式批次路线 ⓪→①→②→②.5→③→④a→④b | council 裁决（alpha/gamma 有效，beta FAILED） |
| 2026-08-18 | BLOCKER-2（`_prepare_data()` 静默绕过）提前到批次①，严重度高于原 P0-4 | alpha 源码发现 run.py:274-280/538-547 |
| 2026-08-18 | 批次④拆分为 ④a/④b，新增批次②.5 | council 裁决（BLOCKER-3） |
| 2026-08-18 | GitHub 仓库设**私有**；远程 origin 已配置 | 用户拍板；推送待网络 |
| 2026-08-18 | **废弃文件不直接删除**，移 `_deprecated\` 登记封存，禁止后续开发参考 | 用户拍板 → 宪法 S7 |
| 2026-08-18 | 结构/命名/子目录卫生约定 + 文档化进度纪律写入宪法（第四/五/六节） | 用户拍板 |
| 2026-08-18 | 加速裁决：预聚合✅ / 指纹分级缓存✅ / Parquet双写✅ / 向量化✅；增量ETL❌ / Polars❌ / DuckDB ③再定 | council 裁决 |
| 2026-08-18 | 批次⓪基线冻结：数据月份 2026-06；silver 4 + gold 30 表共 2,984,033 行；看板 44 顶层变量 / 6969 标量 KPI；freeze 时代码 HEAD=be032e8 | `baseline\20260818\summary.json` |
| 2026-08-18 | 依赖锁版本上限（pandas>=2.2,<3.0 等 10 项，基于生产环境已验证版本） | 宪法 A3 |
| 2026-08-18 | 基线跑批审计发现 `[FAIL] Raw=Silver验证`：Raw YTD rev=43080.6万 vs KPI rev=43081.0万、profit 14832.5 vs 14833.0（约 0.01% 级）——记为既有特征，批次①"审计项真实化"时一并核查 | run_chain 审计输出 |
| 2026-08-18 | golden_diff 容差定为：数值相对 1e-6（近零绝对 1e-6），row_count/列名/schema_hash/字符串/日期严格；exit 0/1/2 | 未决事项 #2 落定 |
| 2026-08-19 | 批次① force 对拍 6 处漂移裁决为**预期内漂移**：同源于 T2——"未知客户"（近12月收入453万/前12月1018万）属性由 NaN 归位（翁创伟/代理/MM层级），其交易进入 price_deep_dive 统计（合并客户全景属性，L268/L416）→ 市场中位价/整体均价微移 → 51 笔交易跨分类边界（MM异常低价+51/偏低关注-51）、细分均价+1.7%；客户全景两行分数微变（该客户自身重评分）。方向=更正确 | golden_diff + 源码逐行核查 |
| 2026-08-19 | P0-1 范围扩大记录：V2 验收发现 KPI 阶段同类缺陷——缓存路径重读 Excel 缺 fillna("未知客户")，nunique 丢 NaN 致客户数日差累计 356（收入一致）。已修复（缓存分支补 fillna，force 分支零改动），修复后缓存/force 零漂移 | 批次① V2 验收 |
| 2026-08-19 | 指纹函数行尾归一化硬化：LF/CRLF 落盘差异不再触发误失效（改配置→失效✓，git checkout 还原→哈希复现✓） | 批次① V3 验收 |
| 2026-08-19 | 审计项 `[FAIL] Raw=Silver验证` 根因查明：检查的是旧版 HTML 字面量 `ytd_rev=`（V9/V10 改 JSON 注入后已消失），恒 FAIL 与数值无关；另含取整差（看板取整到万 vs 审计1位小数）。批次①不动 dashboard，移入批次②修 | T6 调查 |
| 2026-08-19 | 事故登记：编排者在有未提交改动的文件上执行 `git checkout --` 误冲 settings_product.py 两个配置键（已补回）——规则：验收期做破坏性测试前先提交或 stash | 批次① 事故记录 |
| 2026-08-19 | 观察：缓存模式 1479.9s ≈ force 1500.2s，因 KPI 阶段缓存路径仍重读 230MB Excel（run_kpi_daily.py 独立读取路径）。列批次②评估项 | 批次① 验收实测 |
| 2026-08-19 | 批次②验收通过：force 1389.9s / cache 1348.0s 双零漂移（对拍 baseline\20260818_batch1）；HTML 对拍零漂移；2027 纯函数自测 PASS | 批次② 验收 |
| 2026-08-19 | 乱列序验收改静态证明：generate_dashboard.py 位置取列零残留（grep 验证），Excel 列访问已全部语义名化；e2e 打乱列序测试因模块 import 副作用超时放弃 | 批次② 验收方法调整 |
| 2026-08-19 | --skip-processing 单项耗时未单独测量（原预期降30%+未验证）；实测总耗时 force -7.3% / cache -8.9%；看板大提速留批次③预聚合 | 批次② 验收实测 |
| 2026-08-19 | 既有非确定性登记（批次②.5 治理）：①cross_sell.py:81 set 迭代致同分推荐跨进程非确定（19/3143）②PROD_CHANGE/F_PRODUCT_LIST 等值项排序随 PYTHONHASHSEED 漂移 ③shared/pricing.py _compute_batch_metrics 41-49s 成画像最大瓶颈 ④run_action_suggestions 569s 为处理侧最大单点 | 批次② 车道B/C 发现 |
| 2026-08-19 | 批次②.5验收通过：全量 799.3s（批次①基线 1500s → 累计 **-47%**）零漂移；看板输出跨进程逐字节可复现（双哈希种子验证） | 批次②.5 验收 |
| 2026-08-19 | 速度大单点告破：build_monthly_product_trend 536→1.0s、_compute_batch_metrics 73.5→0.45s、run_action_suggestions 631→15.7s；剩余热点 generate_actions_v2 8.8s / generate_cross_sell 5.9s（深度分支业务逻辑，安全向量化风险高，不强改） | 批次②.5 车道B/D |
| 2026-08-19 | **教训入宪法 S8**：等价性对拍必须用生产加载路径（load_silver_table 的 categorical/float32/observed=False 语义）——车道D首版用 read_csv 默认推断 dtype 做对拍，漏过 6 处漂移，被 golden_diff 门禁拦截后按生产路径修复（observed=False/dtype 对齐） | 批次②.5 门禁拦截事件 |
| 2026-08-19 | 死代码裁定修正：诊断报告称 classifiers/risk_scoring 函数"从未调用"失准——实际经 shared/__init__.py re-export 且被 profiling.py:822/830 与 test/validation_suite.py 调用，依"有引用不得删"保留；9 个真死文件已入 _deprecated | 批次②.5 车道A |
| 2026-08-24 | 批次③预演交付：D-2 过滤会高估收入 0.50%（全期）/0.58%（YTD）、0 符号翻转 → 建议保留负数量；D-1 数据层钳制影响可忽略（毛利+0.16%、风险仅1产品迁移）而图表压缩问题真实（毛利率轴被 -662% 极端值拉宽 7.7 倍、有效数据压到 1/8 绘图区）→ 建议仅展示层 p1~p99 截断、阈值入配置；D-4 config.xlsx 不存在、建议本期不引入。全部组合与现行口径差异 <1% | `口径决策预演_20260824.md`（头部三锚点对账通过） |
| 2026-08-24 | **用户拍板（业务门禁解除）**：D-2 保留负数量（维持现状，配置开关保持 False）；D-1 仅展示层 p1~p99 截断、阈值入配置、数据层不篡改利润；D-4 本期不引入 config.xlsx | 用户 2026-08-24 答复 |
| 2026-08-24 | 预聚合方案调整：弃"处理层 export 重算 + 看板消费"双真源设计，改**看板自缓存**（generate_dashboard.py 算完写 output/dashboard/preagg.json + 指纹戳，新鲜则跳过重算）——同一计算路径、零双重真源漂移风险；"--dashboard-only 数据过期报错退出"由指纹门禁实现；看板零 Excel 读取的彻底收口并入④a | 编排者方案修正（原设计漂移风险高） |
| 2026-08-24 | 批次③验收通过：全量 801.7s 零数值漂移；看板自缓存上线（全算 ~400s → 缓存命中 ~1s，逐字节一致）；--dashboard-only 总耗时 9.1s（指纹门禁+缓存渲染），过期/缺失场景 exit1 中文报错；dept_md 纳入指纹第 6 键 | 批次③ 验收 |
| 2026-08-24 | 看板耗时构成实测（DASH_TIMING）：D面销售能力 241.7s（61%）为最大热点，E面 48.7s、Excel 加载 47.7s、F面+C面 27.5s、A/B面 27.4s——④a 收口时优先处理 D面 | 批次③ 车道D 测量 |
| 2026-08-24 | 事故登记：预演合入后未切回批次分支即派发车道，车道改动落在 master 工作区——发现后 checkout 携回 batch/3-caliber-preagg，无损失；规则：派发车道前先确认当前分支 | 批次③ 纪律事故 |
| 2026-08-24 | 推送恢复：master dc3e2eb→6d7075f + pre-batch-3/batch-3-done 已上 GitHub | 用户网络恢复 |
| 2026-08-24 | D-1 截断可复用性确认（用户提问）：**非硬编码**——阈值在 config `DASHBOARD_AXIS_CLIP`（分位数可调）；边界每次运行用 np.percentile 对当前数据现算（`_compute_percentile_bounds`/`_margin_axis_bounds`/`_asp_axis_bounds` 辅助函数），经占位符注入模板；新增图表复用方式=调辅助函数+加配置键。数据变动→边界自动跟随 | 批次③ 车道D 实现 |
| 2026-08-24 | 看板自缓存价值边界确认（用户观察，正确）：数据更新（新月份/基础数据变动）必须全量重算，缓存快路径只服务看板代码/模板/截断迭代（400s→1s）；数据正确性优先于速度的设计不变 | 用户反馈 |
| 2026-08-24 | 批次④a验收通过：全量 618.6s（批次①基线 1500s → 累计 **-59%**）；看板全算 ~400s→100.9s（D面 241.7→1.4s、Excel 加载 47.7→3.4s）；零数值漂移 | 批次④a 验收 |
| 2026-08-24 | ④a 集成缺陷修复：车道D指纹 excel 键置空 vs 车道P门禁含 Excel → --dashboard-only 恒过期；统一为两侧纳入 Excel 身份（新月份未全量跑时必须报错，拍板#3语义） | 批次④a 验收拦截 |
| 2026-08-24 | **方法学教训**：golden_diff 对对象数组只比 array_len 存在盲区——④a 两个潜在缺陷（D面 prod_line_col 子串误命中"型号_产品线（新）"、品类候选顺序致 --from-excel 错列）由 --from-excel 逐字节对拍发现（修复后逐字节=权威参照 258475…）。规则：加载底座级改动必须加逐字节对拍，不能只靠 golden_diff | 批次④a 车道修复 |
| 2026-08-24 | 批次④a方案调整（登记偏离）：逆向工程发现 13 列全部已在 silver（列级零缺口），从"逐面迁 Gold"改为"加载底座单点切换"；原计划的 4 张预聚合 gold 表未建（D面 groupby 直消费 silver 已达标，YAGNI） | DASHBOARD_FACE_MAP.md 证据 |
| 2026-08-24 | 批次④b验收通过：全量 764.9s 零数值漂移；pytest 7 用例全绿；run_all 显式 STAGES 注册表化完成；[STAGE] 协议零改动 | 批次④b 验收 |
| 2026-08-24 | 观察：④b 全量 764.9s vs ④a 618.6s，差异为运行波动（230MB Excel 读取/系统负载），非结构性回退；结构性收益不变（看板段 ~101s、dashboard-only ~1s） | 批次④b 验收实测 |
| 2026-08-24 | **全部批次（⓪①②②.5③④a④b）执行完毕**，系统进入常态维护：月度跑批用 run_chain.py --force-silver；看板迭代用 --dashboard-only（~1s）；每季度跑配置/硬编码审计（施工总计划 §7.3） | 项目阶段结论 |
| 2026-08-24 | git 环境根因查明：新 Git 客户端（2.55.0.windows.5）08-20 装于 `AppData\Local\Programs\Git`，注册表 PATH 已含但**旧 shell 进程不继承**（本会话 git 中途失效的根因）；github.com 凭据存于 Windows 凭据管理器（`git:https://github.com`）；后续会话用新客户端全路径或新开 shell | 用户提示 + 环境复查 |
| 2026-08-24 | 可选项取消（用户拍板：意义不大可不做）：季度审计脚本化、看板剩余热点（E面46.9s等）优化、壳侧 catch_unwind、actions_v2/cross_sell 深度优化 | 用户拍板 |
| 2026-08-24 | **五议题评估轮交付**（`新增需求评估与讨论_20260824.md` + 设计师 `docs\看板叙事与扩展设计评估_20260824.md`）：①存储设计实测——ERP Excel 当前为明文（DSE 未触发）、Excel 读取占全量跑 <5%，建议"快照仓"轻量方案（DSE 风险隔离而非速度）；②③④设计/侦察方案就绪；⑤耦合地图清晰但发现 forecasting/utils 死路径真 bug（指向已删除目录，跑必崩） | 评估轮（exp+designer+实测） |
| 2026-08-24 | **DSE 实测（7月文件）**：`财务分析-7月（8.1）.xlsx` 已被 DSE 加密（头 00 00 5b 00）；calamine/openpyxl 读不了加密件；COM 解密读 62.3s/次（打开 47.9s+读值 14.4s，199,687 行×71 列）；现役 read_excel_auto 无加密兜底→**下月跑批现在就会崩**。W1 快照仓优先级升为「高」（下月跑批前置），实施规划已修订 | 用户供文件实测 |
| 2026-08-24 | 用户拍板（新批次规划方向）：待办+行动+口径说明**合并为一个新面**、先出人工确认后写入的测试版；叙事结构做"每面内容与主题严格相符"详细设计；耦合只修 kanban↔主项目、forecasting 只记录（暂时不用）；CoStrict 删除（按 S7 封存）；其他接受。规划文档 `新批次实施规划_20260824.md` 待评审 | 用户拍板 |
| 2026-08-24 | **仓库大扫除**（用户指令：递归挨个文件夹检查）：释放 **7.4GB**——kanban-runner target 9.5GB 构建产物、portable-python 垃圾 ~230MB（__pycache__ 928目录/151MB、Doc 47.8MB、test 22.3MB、idlelib/pydoc_data/ensurepip/turtle）、node_modules 59MB、全仓 __pycache__/.pytest_cache；CoStrict 与 forecasting 报告产物 5 个按 S7 封存并登记；原则确立：**再生类（缓存/构建产物）直接删，归档类（有参考价值）进 _deprecated** | 大扫除执行 |
| 2026-08-24 | **共享安全加固**：共享盘两处泄露已删（内部分析报告 md + 全量数据看板 dashboard_a.html 12.8MB）；publish 排除清单增加 dashboard_a.html / preagg.json / data_warehouse——原则：**共享盘只出核心代码** | 用户拍板 |
| 2026-08-24 | 新面再合并（用户修订）：异常与风险+行动与说明合为一面「**风险与行动**」（"没那么多内容写不用分开"）；F面 H1专项加开关（按需更新+可视展示开关）。规划文档已同步修订 | 用户拍板 |
| 2026-08-24 | **加密读取策略与数据库方案结论**（议题4）：现状=silver 层已全 parquet 化（批次②④a），但 ingest 入口（Excel→silver）仍逐次读 Excel（calamine 明文可用/加密即崩无兜底）→ W1 快照仓补齐；**数据库方案评估=不立项**（证据：Excel 读占全量跑 <5%、COM 解密税仅 ingest 付一次 62.3s、parquet 读 1.5s 已达标、零新依赖；DuckDB/SQLite 留触发式评估：数据量上台阶/多源并发/需 SQL 时） | 评估结论 |
| 2026-08-24 | **W1 快照仓落地**（优先级：下月跑批前置）：`scripts\ingest_snapshot.py`（明文 calamine ~25s / 加密 COM 62.3s 一次）+ manifest 对账 + run_all 读侧快照优先 + 加密无兜底报错引导。实测：6月明文 ingest 对账全等（191,725行）；7月加密 COM ingest 成功（199,681行）；快照端到端重建 silver 与基线逐表全等；golden_diff CSV 零漂移 | W1 车道验收 |
| 2026-08-24 | **W2 kanban 侧落地**（kanban 仓 commit d87210a）：publish 修 vnogit（git 全路径回退）+ 版本自检（已最新/落后 N 版/-Force）+ UNC 单点配置化（share_config.json）+ AGENTS.md 发布核对清单；主仓侧：宪法 S6 扩写发布纪律 + 使用说明补发布段落 | W2 车道验收 |
| 2026-08-24 | 杂项确认：installer 0.3.13 为最新版（kanban 文档"0.3.12"为陈旧记录，待 kanban 仓文档同步）；dashboard_a.html 从共享盘删除不影响生成（纯产物，每跑重建）；全量跑维持 ~10-13 分钟（数据更新必须全量重算，正确性优先） | 用户问答 |
| 2026-08-25 | W3 设计修订 v2（用户评审反馈落地）：A面主题重定义「经营总览=总体+常用维度速览」（跑题区块不迁走，改"速览级保留/明细下沉引导"）；R面双数据源（gold 风险表 + 经营会议 md 人工跟踪项，统一候选→人工确认→写入）；说明体系升级为三层（面级口径条/图表级导读/指标级 hover 术语卡）+ "口径变更必须同步更新 faces.yaml" 纪律；全文精炼 22%（835→650 行） | 用户反馈 + des-1 修订 |
| 2026-08-25 | W3 设计修订 v3（用户二轮反馈）：口径文案规范定为"只写业务含义与关注信号，不写取数与公式"（管理层语言）；H1 语义纠正为上半年（半年度专项，cadence=half_year，visible 人工核定）；R面更新操作流明确为非程序员三步（改 meeting_track.md → 双击跑 --dashboard-only 秒级 → 开看板）；字母面代称弱化（中文面名为主，字母仅内部 face_id）。注：v3 由 des-1 在配额耗尽前写入磁盘，编排者抽查验证质量通过 | 用户反馈 + des-1 修订 |
| 2026-08-25 | W3 设计修订 v4（用户三轮反馈，编排者直接修订）：口径措辞再收——只写简明公式+通俗名词说明，**不写判断标准**（我方阈值未必与管理层一致）；F面平时彻底关闭、半年度复盘期人工核定开启；R面改**总体文档模式**——全部内容（含数据驱动项，可人工调整）汇成月度总体文档 `risk_action_YYYYMM.md`，人工审定后按固定模板渲染，与其他面同风格；会议跟踪项并入总体文档（meeting_track.md 降为可选速记） | 用户反馈 |
| 2026-08-25 | **日期订正（第二次）**：W3 设计文档 v1-v4 全部实为 2026-08-25 上午（git 时间戳 09:10-10:57），误标 08-24——文档改名 `看板叙事结构详细设计_20260825.md`、文内 9 处日期修订、台账同步。规则重申：跨日工作写台账/文档前先 `Get-Date` 对钟 | 用户发现 + git log 核实 |
| 2026-08-25 | **W4 风险与行动测试版落地**（编排者直接实现，设计师/执行者配额 14:00 恢复）：交付 `dashboard\faces.yaml`（7 面注册表+v4 口径文案）、`generate_risk_face.py`（初稿生成+渲染+状态回写）、`template_risk_test.html`（复用正式看板设计令牌与组件）、`action_items.json`。验证全过：初稿策展（同客户合并+每类 Top10，异常 10+负毛利 10 条，溢出 2242+30 条登记）；负毛利符号/等级映射修正（源表负值存储、无/轻微/关注/严重→高/中/低）；md 单元格竖线清洗（建议动作含 \| 分隔符曾致渲染崩溃）；人工审定版不覆盖、编辑后渲染正确反映；正式看板哈希不变 | W4 验证 |
| 2026-08-25 | 待用户评审：打开 `sales_analytics_platform\dashboard\dashboard_risk_test.html` 检查「风险与行动」测试页样式与内容；确认后进入并入正式看板阶段（需调 tab 顺序+导航横幅，届时再评审） | W4 待评审 |
| 2026-08-25 | W4 测试版用户评审通过（链路通即可，内容细节随新数据再调）；顶部 A 面速览条按用户意见移除（与总览决策面重复，R 面只留本面 KPI 卡） | 用户评审 |
| 2026-08-25 | **R面并入正式看板完成**（用户拍板"并入吧"，分支 feature/r-face-integration）：备份先行（pre-r-integration tag + baseline\r_integration_backup\ 三文件）；改动=generate_risk_face 重构出可复用 build_r_face_inner_html + generate_dashboard 服务端注入 %%R_FACE_HTML%% + template 加 R tab（中文面名、弱化字母、无横幅按用户口头说明）+ fingerprint 加 risk_doc 键（**内容级哈希**——mtime 语义曾致"还原误判过期"）。验收：全量 623s 对并入前备份逐字节零漂移；过期/还原门禁三场景全过；pytest 7/7 | #1 验收 |
| 2026-08-25 | **#2 七月跑批实战完成**（DSE 加密全链路实战）：文件名编码坑（shell 中文传参损坏→快照名不匹配→加密守卫正确拦截）用 Python 驱动脚本绕开；快照扩展覆盖客户信息表 sheet（7月源文件无此 sheet→build_cust_info 回退，与明文口径一致）。7月全量 525.9s exit=0、审计全过、YTD收入5.084亿、最新数据日 2026-07-31；R面初稿 risk_action_202607.md 已生成（异常10+负毛利10条，负毛利损失331.0万）待人审定；7月基线冻结 baseline\20260825_july | #2 验收 |
| 2026-08-25 | 指纹设计修正（#1 收官后复盘发现）：risk_doc 不入指纹 + R面HTML两路径现算（同 C面毛利率轴"永远现算"模式）——否则"改审定文档→秒级重渲染"会被门禁误拦要求全量跑；修正后三场景验证全过（改文档→放行且内容反映） | #1 设计修正 |
| 2026-08-25 | **#3 插拔机制落地**：faces.yaml 面开关（enabled/visible）接入 generate_dashboard.py——F 面 8c-8h 连续块（432 行）机械化包裹进 `if _face_visible("F")`，else 兜底 13 个块后引用变量（静态分析列全：11 个 F 变量 + _asp_bounds + _asp_axis_cache，后者漏兜底曾致缓存写入静默失败、门禁误判过期）；不可见面服务端移除 tab 按钮+JS 监听器；faces.yaml 入指纹 settings 键。验收：F 关闭时看板快 28s（110s→82s）、F_* 空对象、pytest 7/7、可逆性闭环（翻开恢复 13 键/翻回隐藏）、golden_diff 43 处漂移全部 F_*（预期）。另修取数口径一致性隐患：find_source_data/run_kpi_daily/run_pipeline 三处"字母序取第一个"统一为 mtime 最新（多月度文件并存时取错的风险） | #3 验收 |
| 2026-08-25 | **#4 小事清单完成**：git 署名配置（两仓库 repo-local：L-iuhuan / 910373@toll.cn）；kanban 文档版本表同步 0.3.13 为当前生产版（kanban 仓 commit 956c279，已推送）。**四件事（R面并入/7月跑批实战/插拔机制/小事清单）全部完成** | #4 验收 |
| 2026-08-25 | **#2 数据准确性核查结论**（用户提问"改完以后不对了"）：当前 7月看板 vs 7月基线仅 F_* 预期漂移（插拔关闭所致）；程序时代（batch⓪起）零解释外漂移；旧版 __pycache__ 副本 vs 当前的差异全部归类——①批次① P0-1 口径归一（客户集合 3197→3143、销售员 27→26、各列表剔除空值/伪条目，方向=更正确，已裁决）②批次③轴截断新增变量（拍板）③模板演进（TABS/F_DEPT_EXPANDED 新增、B_QTY_MODE 移除）④**B_TREND 稀疏客户趋势各少 1 个月**：batch⓪基线（08-18）已存在，M 重组时代（7/27）与②基线一致（m3_verification 记载"5 个差异均非数据"），属本项目开工前的远古时代差异——非本项目引入，如需考古可另立项（_archive_source 有旧码） | #2 核查 |
| 2026-08-25 | **#1+#3 模板修复**（用户报障）：R tab 点击无反应根因=监听器清单漏 btnR（已补）；右上角导入/导出/撤销三件套+TABS.importer+SheetJS 移除（用户拍板无意义）。验证 12/12 PASS | 用户报障修复 |
| 2026-08-25 | **仓库级隐患修复**：template.html 被 .gitignore 的 *.html 误伤从未跟踪——批次②占位符化/R面tab/三件套移除全在磁盘裸奔。已加白名单例外入库（58fdca6）。教训：核心源文件入库状态应列入季度审计 | 仓库卫生 |
| 2026-08-25 | **#4 渲染性能优化**（设计师实施）：C面图表懒加载（首访才建11个实例）、B面客户列表分页（200/页+加载更多）、resize 只作用可见图表、A面回访减重。验收：pytest 7/7；golden_diff 45 处漂移全部查明归属（43 F_* + 2 ASP_AXIS 为插拔 F 关闭预期 + 1 B_LIST_LIMIT 为分页新增变量），无未知漂移 | #4 验收 |
| 2026-08-25 | **口径说明体系落地**（用户发现漏挂待办）：faces.yaml 补全 A-E 面 sections（v4 规范措辞）+ 每面顶部可折叠口径条（默认收起）+ 主要图表一句话导读 + 术语表字段（暂不进 UI）。验收：golden_diff 45 处全为已登记预期漂移、pytest 7/7、五面口径条探针全过。R 面原有口径节不动 | 用户发现缺口 |
| 2026-08-25 | 需求回顾盘点（用户要求查会话记录）：历史讨论事项全部核对——已完成的见批次/W系列/四件事记录；**遗留仅两项**：①B_TREND 远古差异考古（可选，用户说需要时再验证）②0.3.13 安装包上共享盘（publish -AppInstaller，用户决定时机）。其余全部闭环 | 需求盘点 |
| 2026-08-25 | R面 KPI 卡片居中修复（4 张卡 max-width 960px 居中，inline 样式不动模板结构）；正式看板经 --dashboard-only 重渲染已生效 | 用户反馈 |
| 2026-08-25 | **新品渗透率差异查明**（用户报"只有2.3%，和之前相差很大"）：双口径实测——ERP"是否新品"标记口径 6月=12.2%/7月=12.7%（旧版看板口径）；看板现行口径=标记∩产品近12月首发=2.1%/1.6%，**83% 的标记新品收入被"近12月首发"过滤**；该过滤（cutoff_new）在 M1 平台合并（2026-07-27）引入，早于本修复程序基线（batch⓪ 08-18），故门禁不可见。证据链完整，列入未决#13 待拍板 | 数据核查 |
| 2026-08-25 | 两项评估交付：`project_analysis\性能与产物评估_20260825.md`（计算侧已到位；产物裁剪空间=提价机会.csv 170MB 无看板消费方 + report\ 56.6MB/次不清理累积 283MB；3 个待拍板项）+ `docs\设计语言精进评估_20260825.md`（tabler 调研+差距分析+8 个改进点+不动技术栈路径） | 评估交付 |
| 2026-08-26 | **合并提速分支 `perf/20260825-speedup` 完成并验证**（merge 60fe2a8，merge-tree 零冲突）：⑤ P1 看板 groupby 预分组（~2550 次全表扫描→一次）+ 缺陷A 输出路径统一 config.settings + 缺陷B pandas 锁<2.3.2（我原 requirements 允许 2.3.2+ 是雷）+ ⑥ ETS 多进程/gold 子模块向量化/节假日缓存/Excel 导出优化 + ⑦ Excel 报告开关 + 提价机会过滤。用户拍板：提价机会只留有效行、Excel 默认关。**关键修正：产品生命周期报告 EXCEL_REPORT.product_enabled=True 保留**（分支文档"两份报告不被看板消费"对 C面不成立——C面 load_product_report 依赖其 history/sankey/月份，关掉会退化到无历史数据），客户分析报告=False 关闭 | 合并决策 |
| 2026-08-26 | 合并后验证全绿：端到端 **526s→254.7s（-52%）**（看板 116s→70.9s）；提价机会 505 有效行与合并前有效子集**逐格一致**；产品生命周期报告今日已生成（C面正常）；客户分析报告无新增（开关生效）；golden_diff 55 处漂移全部归因（45 已登记 F_*/ASP_AXIS/B_LIST_LIMIT + 10 提价机会过滤预期差异，**0 待查明**）；pytest 7/7。新增未决#14：observed=True 口径统一（category 叉积 263 万行是否该只算实际组合，分支作者建议另行立项，需业务拍板） | 合并验证 |
| 2026-08-26 | **设计语言第一轮入库为生产版**：design/v2-tokens（d39d127，卡片两级/表格统一alias/色彩语义/tab选中态/KPI卡规范/间距阶梯/空状态）合入 master 推送（b6a9c64..d39d127），验证 golden_diff 55 处已登记漂移不变 + pytest 7/7 | 设计收敛 |
| 2026-08-26 | **设计第二轮**（efbf0dc，用户反馈"搜索框/按钮/面包屑/斑马线/对齐/下拉/图表再看下"）：搜索框统一宽度+focus ring、按钮尺寸阶梯+focus-visible+active、面包屑经评估不加（单页tab结构重复）、斑马纹统一、tabular-nums 数字对齐、下拉统一、ECharts 主题 token 化 | 设计精修 |
| 2026-08-26 | **设计第三轮**（1eb69e1，用户反馈工具栏并排/斑马纹颜色/图表遮挡裁切）：B面工具栏并排、斑马纹改中性灰 #F6F7F9（亮度差2.5%存在但不抢戏）+hover 深蓝可区分、全图表 containLabel/confine/avoidLabelOverlap/边距放宽 | 设计精修 |
| 2026-08-26 | **设计第四轮**（e46c317，用户 12 项反馈）：B面工具栏四组件等宽 160px 并排（浏览器实测 160×4）、多图轴名 nameGap 防裁切（根因=ECharts containLabel 不含轴名，kaaaLine/asp/matrix/scatter/bubble/瀑布×2/散点/导入图）、采购节律框统一白底细边、毛利诊断压缩两排（3+4格）、渐变全部去除（短板诊断/本月建议/C面统计框）、品类热力图 Top15销售员+口径注、客户利润热力图 Top15+p95色阶截断+白描边+口径注（治雾蒙蒙）、导入图字号 8/9→10/11px+图例。验证：重生成全链 OK、r3vsr4 数据零漂移、JS 语法 OK、14 张截图逐项核对通过 | 设计精修 |
| 2026-08-26 | **第四轮多智能体辩论评审**（用户要求）：三议员（alpha 完整性/beta 信息设计/gamma 实现质量）+council 综合，五席交叉验证。编排者事实核查裁决：tooltip fallback 索引 bug 证实（本轮引入，必修）、`_cust` Silver 路径显示编码论断**证伪**（产物实测全为可读简称）、CSS 宽度冲突 150vs160 证实、glossary 声明未渲染证实（faces.yaml 7 处 vs _build_face_meta_html 零引用）、fix_d_heatmaps.py 等 4 脚本=des-1 配额中断遗留探针已删。**必修 5 项全部落地**：tooltip 索引修复、四组件统一 160px 去 !important、F 面补口径条（占位符+缓存元组+替换表三处）、品类热力图备注措辞、eDrawerTrend 补 containLabel。终验：数据零漂移+JS OK+落地探针 PASS+浏览器实测等宽。议会 backlog 入未决#15 | 议会评审 |
| 2026-08-26 | **第五轮**（08332c1，用户批评"评审问题未修完"后补齐）：用户 3 项——拆解瀑布图容器 130→210px（grid 扩边后绘图区仅剩 60px 的压扁根因）、毛利诊断卡闭合 div 修复（r4 删 grid 包裹时误删闭合标签致新品表现嵌套贴住，DOM 实测脱离嵌套+间距 14px）、B面工具栏最终方案 flex 等分+nowrap（120px 固定宽在窄面板折成 2×2，obs-3 截图核验抓出；改为 flex:1 1 0 等分恒等宽单行，CSS 规范级保证）。**议会遗留 8 项同轮补齐**：glossary 术语卡渲染（_build_face_meta_html 补 glossary 块，三层口径体系闭环）、下拉统一 form-select 六处（bTier/bAct/bRisk/eYear/eTier/dTrendSelect）、.rgx 去 !important、热力图 Top15/p95 截断注释、6000颗表述统一（单次 vs 当月两种口径显式区分×6处）、D面补 custHeatmap/waterfall 导读（模板占位符+yaml 同步）、A级定义量化（改为"绩效数据能力等级字段"数据源声明，不编造阈值）、heatmap 导读同步 Top15 口径。验证：重生成 74.2s 全链 OK、r4vsr5 数据零漂移、13 项落地探针 PASS、JS OK、瀑布图/卡片间距截图核验达标。教训记录：议会评审产出应当场消化小项，登记 backlog 不等于免修 | 设计精修 |
| 2026-08-26 | **第六轮**（7442892，用户反馈"§3量利拆解左右高度不一/上下布局乱"）：结构重排——左列改 flex 列+排名表 flex:1 自动填满（grid align-items:stretch），与右列瀑布图 210×2 精确等高（DOM 实测 473=473px）；上下间距统一 10px 节奏；散点图保持全宽居下。obs-3 上下两段截图核验达标（左右底部对齐/散点图与灰框完整协调）。验证：重生成 74.8s 全链 OK、r5vsr6 数据零漂移 | 设计精修 |
| 2026-08-19 | GitHub 推送成功（master + pre-batch-0/1 + batch-0/1-done 共 4 tags） | 用户网络恢复 |
| 2026-08-19 | 批次②开工：参照基线 `baseline\20260818_batch1`（summary 入库 + dashboard_a.html 本地留存作 HTML 对拍基准）；验收门槛=golden_diff **零漂移**（批次②全部改动必须行为保持）；三车道并行（A=看板文件 / B=处理侧向量化 / C=Parquet双写+KPI评估） | 施工总计划 |
| 2026-08-24 | **日期订正**：会话锚定环境初始日期（08-19）未随天数推进对钟，批次③及以后的记录误标 2026-08-19。以 git 提交时间戳为准全面订正：批次⓪=08-18、①/②/②.5=08-19、③/④a=08-24；预演报告改名 `口径决策预演_20260824.md`。规则：跨日工作写台账前先对系统时钟 | 用户发现 + git log 核实 |

## 未决事项（等用户/业务拍板）

| # | 事项 | 建议 | 阻塞什么 |
|---|---|---|---|
| 1 | ~~D-1 毛利率钳制 / D-2 负数量~~ **已拍板 2026-08-24**：D-2 保留、D-1 仅展示层 p1~p99 截断（已落地） | — | — |
| 2 | ~~golden-diff 容差~~ **已落定**：数值 1e-6 + 计数严格 | — | — |
| 3 | ~~--dashboard-only 数据过期行为~~ **已落地**：过期报错退出（指纹门禁） | — | — |
| 4 | ~~根目录两个 rar + 散件 xlsx 处置~~ **已解决 2026-08-24**：rar 维持登记保留；两个散件 xlsx 移入 `_deprecated\` 并登记（含7月附件的取回指引） | — | — |
| 6 | D-4：`load_config_from_xlsx` 是否有人维护 config.xlsx | ~~随 D-1/D-2 一并问业务~~ **已拍板 2026-08-24：本期不引入**（config.xlsx 不存在） | — |
| 7 | **远期构想（只记录不实施）**：空闲时把 Excel 数据导入数据库、跑批改从库读，绕开 DSE 加密 Excel 的读取开销与限制 | **触发式立项，不设固定日期**。触发条件（用户拍板 2026-08-24）：①ERP 导出方式变化或数据量上台阶；②Excel 读取成为实测瓶颈或 DSE 加密限制咬人；③下一大型版本（新看板面/新数据源）立项时顺带评估。环境注意：DSE/亿赛通透明加密约束 | 远期（触发式） |
| 8 | T1 快照仓小批次（DSE 风险隔离：月度 Excel 落 data\ 后即刻转 parquet 明文快照，跑批读快照） | 建议立项，优先级中 | 用户拍板后开工 |
| 9 | T2 看板口径说明（faces.yaml）+ G面行动清单（action_items.json 入库+维护人） | 设计评估已出；待拍板维护人 | 用户拍板后开工 |
| 10 | T3 共享盘同步强制化（纪律入宪法/台账 + publish 版本自检 + 路径单点配置化 + 使用说明补发布段落） | 建议全做 | 用户拍板后开工 |
| 11 | T4 叙事重排 A→G→B→C→D→E→F + 各面更新节奏/插拔（faces.yaml 注册表） | 待拍板（影响用户习惯） | 用户拍板后开工 |
| 12 | T5 forecasting 治理：①utils 死路径（跑必崩）修或封存；②C2 排行榜依赖文档精确化、C3 死引用清理、C4 数据源统一；③是否补 forecasting 最小基线 | 在用→修；不用→封存 _deprecated；C2-C4 建议一并 | 用户确认 forecasting 各子项目是否还在用 |
| 13 | ~~新品渗透率口径~~ **已拍板并落地 2026-08-25**：改回纯 ERP 标记口径（12% 级）。真实修复位置=A面卡 JS 数据源 `npctVal`（原 `sum(B_CUSTS[].new_ytd_rev)`——四重过滤：标记∩近12月首发∩销量≥6000∩YTD首发=1.6%），改为 Python 纯标记占比经 `%%NEW_PCT_PURE%%` 直接注入，实测卡面值 12.7%；附注：Python 变量 new_pct 同步改纯标记口径（该变量 %%NEW_PCT%% 在模板中零使用，为死代码一致化）；E面/F面口径不受影响（各自概念不同） | — |
| 14 | observed=True 口径统一：categorical groupby 默认 observed=False 展开客户×产品叉积（提价机会 2,633,504 行幽灵行、NaN/0 值，仅 505 行有效）。批次⑦过滤到 505 行是止血；根源是 observed=True vs False 的口径选择（分支作者建议另行立项） | 建议：与业务确认"提价机会"语义应只算实际组合（observed=True）后改代码 | 用户/业务拍板 |
| 15 | **设计议会 backlog**（2026-08-26 第四轮辩论评审产出，按性价比排序）：⭐⭐⭐ ①渲染 glossary 术语卡（faces.yaml 7 处已声明但 _build_face_meta_html 不渲染，~15 行代码闭环三层口径体系）②量化模糊术语（"A级=位于前列"无量级、"≥6000颗"三种表述，纯 yaml 编辑）③统一下拉控件 .form-select；⭐⭐ ④C面次级分布图折叠 ⑤清理 .rgx !important ⑥热力图 Top15/p95 逻辑加注释 ⑦A面 saList 全量渲染确认量级 ⑧统一排序 affordance；⭐ ⑨chartDefaults 统一层（75处fontSize/35处grid，2-3人日专项）⑩口径文案机制统一 ⑪C面11图表分帧初始化 ⑫A面信息过载治理（子KPI下沉） | 建议按⭐数分批立项，①②③合计<2小时可随下轮顺手做 | 用户拍板优先级 |
| 5 | ~~GitHub 推送~~ **已解决 2026-08-18**：master + 4 tags 推送成功 | — | — |

## 偏离记录

| 日期 | 批次 | 偏离内容 | 原因 | 影响 |
|---|---|---|---|---|
| 2026-08-18 | ⓪ | "阶段产物 schema 预检"未做独立校验脚本，以 golden_diff 的严格 schema 对比（row_count/columns/schema_hash）+ DATA_CONTRACT.md 替代 | 功能重叠，独立脚本增量价值低 | 批次①再评估是否需要独立预检；不影响⓪验收 |
| 2026-08-19 | ② | 乱列序 e2e 测试改静态证明；--skip-processing 降30%+ 未单独测量 | generate_dashboard import 副作用致 e2e 超时；总耗时改善已实测（force -7.3%/cache -8.9%） | 不影响核心验收（双零漂移+HTML对拍）；批次③预聚合落地时补测 dashboard-only 耗时 |
